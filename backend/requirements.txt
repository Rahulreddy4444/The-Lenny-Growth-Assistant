# FastAPI
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
pydantic-settings>=2.0

# Database
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
psycopg2-binary>=2.9
pgvector>=0.3

# LLM Providers
anthropic>=0.40
httpx>=0.27

# Agent SDK (primary agent layer — falls back to anthropic SDK if unavailable)
claude-agent-sdk>=0.1

# Utilities
pyyaml>=6.0
requests>=2.31
fastembed>=0.2.0

# HTML Sanitization
nh3>=0.2

# Logging
structlog>=24.0
